urls:
  api: "https://devnet-api.multiversx.com"
  swagger:
    - "http://localhost:3000"
itheumSdk.environment: "devnet"
api:
  auth: true
features:
  publicApi:
    enabled: true
    port: 3000
  keepAliveAgent:
    enabled: true
nativeAuth:
  maxExpirySeconds:
  acceptedOrigins:
    - "http://localhost:3000"
rateLimiterSecret:
keepAliveTimeout:
  downstream: 61000
  upstream: 60000
